<%= form_for @honorario_minimo, :html => {:class => 'x'} do |f| %>
  <% if @honorario_minimo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@honorario_minimo.errors.count, "error") %> prohibited this honorario_minimo from being saved:</h2>

      <ul>
      <% @honorario_minimo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
    <div class="col-md-6 col-md-offset-3">
      <div class="form-group">
      <%= f.label :tipo_visado %><br>
      <div class="col-sm-4" style="padding:0">
      <%= f.select :tipo_visado, TIPO_VISADO,{} , {class: 'form-control'} %>
      </div><br>
    </div>
      <div class="form-group">
        <%= f.label :concepto %><br>
        <%= f.text_field :concepto,  class: 'form-control', :required => true %>
      </div>
      <div class="form-group">
        <%= f.label :desde ,"Desde Bs.F"%><br>
        <%= f.number_field :desde,  class: 'form-control', :required => true %>
      </div>
      <div class="form-group">
        <%= f.label :hasta ,"Hasta Bs.F"%><br>
        <%= f.number_field :hasta,  class: 'form-control', :required => true %>
      </div>
      <div class="form-group">
        <%= f.label :porcentaje ,"Porcentaje %"%><br>
        <%= f.number_field :porcentaje,  class: 'form-control', step: 'any', :required => true %>
      </div>
      <CENTER>
      <div class="actions col-md-12">
      <%= f.submit 'Aceptar', class: 'btn btn-primary'  %>
       </div>
       </CENTER>
     <% end %>
    </div>

<script>
  
$( ".x" ).validate({
  rules: {
    field: {
      required: true
    }
  }
});

</script>
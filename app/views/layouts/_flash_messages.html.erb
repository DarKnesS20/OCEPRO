
<% if !flash.empty? %>
<center>
  <% flash.each do |name, msg| -%>
    <p class="alert alert-<%= name.to_s == 'notice' ? 'info' : 'danger' %>">
      <%= msg %>
    </p>
  <% end -%>
</center>

<% end %>
